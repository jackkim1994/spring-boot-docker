name: CI/CD pipeline

on:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Check code
              uses: actions/checkout@v4
            - name: Set up Docker Image
              run: docker build -t spring-boot-app-image:latest .